[general]
output_dir = "downloads"
continue_on_error = true
archive_file = "downloaded.txt"
# archive_file обычно создается автоматически самим yt-dlp

[download]
format = "bestvideo*+bestaudio/best"
merge_output_format = "mp4"
filename_template = "%(title)s [%(id)s].%(ext)s"
retries = 30
fragment_retries = 30
extractor_retries = 3
fragment_retry_sleep_sec = 5
socket_timeout_sec = 30
concurrent_fragment_downloads = 1
noplaylist = true
quiet = false
write_thumbnail = false
write_info_json = false

[http]
cookie = ""
# Вместо строки cookie можно использовать Netscape cookie file:
# cookie_file = "cookies.example.txt"
cookie_file = ""
user_agent = ""
referer = ""

[http.headers]
# Authorization = "Bearer your-token"
# X-Requested-With = "XMLHttpRequest"

[targets]
# Можно перечислить ссылки здесь:
# urls = ["https://site.tld/video/1", "https://site.tld/video/2"]
# Или вынести их в отдельный файл:
# urls_file = "urls.example.txt"
urls = []
urls_file = ""
